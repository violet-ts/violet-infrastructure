{
  "name": "@self/def-env",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "_:cdktf": "cdktf --app \"pnpm run --filter=../bot compile && ts-node -T ./src/entry.ts\"",
    "_:cdktf-init-and": "pnpm run cdktf:synth && terraform -chdir=./cdktf.out/stacks/violet-infra init && terraform -chdir=./cdktf.out/stacks/violet-infra plan && terraform -chdir=./cdktf.out/stacks/violet-infra",
    "cdktf:deploy": "pnpm run _:cdktf-init-and -- apply",
    "cdktf:destroy": "pnpm run _:cdktf-init-and -- destroy",
    "cdktf:get": "cdktf get",
    "cdktf:list": "pnpm run _:cdktf-init-and -- show",
    "cdktf:synth": "pnpm run _:cdktf -- synth",
    "test": "run-s test:tsc test:jest",
    "test-watch": "run-p test-watch:*",
    "test-watch:jest": "echo todo: jest --watch",
    "test-watch:tsc": "tsc --noEmit --watch",
    "test:jest": "echo todo: jest",
    "test:tsc": "tsc --noEmit"
  },
  "engines": {
    "node": ">=14",
    "npm": "use pnpm",
    "pnpm": ">=6",
    "yarn": "use pnpm"
  }
}
