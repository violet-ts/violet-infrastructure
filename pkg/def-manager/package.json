{
  "name": "@self/def-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "_:cdktf": "cdktf --app \"pnpm run --filter=../bot compile && ts-node -T ./src/entry.ts\"",
    "_:tf": "terraform -chdir=./cdktf.out/stacks/violet-infra",
    "cdktf:deploy": "pnpm run _:cdktf -- apply",
    "cdktf:deploy:yes": "pnpm run _:cdktf -- apply --auto-approve",
    "cdktf:destroy": "pnpm run _:cdktf -- destroy",
    "cdktf:get": "cdktf get",
    "cdktf:list": "pnpm run _:cdktf -- show",
    "cdktf:synth": "pnpm run _:cdktf -- synth",
    "test": "run-s test:tsc test:jest",
    "test-watch": "run-p test-watch:*",
    "test-watch:jest": "echo todo: jest --watch",
    "test-watch:tsc": "tsc --noEmit --watch",
    "test:jest": "echo todo: jest",
    "test:tsc": "tsc --noEmit"
  },
  "engines": {
    "node": ">=14",
    "npm": "use pnpm",
    "pnpm": ">=6",
    "yarn": "use pnpm"
  }
}
