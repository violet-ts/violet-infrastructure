{
  "name": "@self/shared",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "run-s test:tsc",
    "test-watch": "run-p test-watch:*",
    "test-watch:tsc": "tsc --noEmit --watch",
    "test:tsc": "tsc --noEmit"
  },
  "dependencies": {
    "aws-sdk": "^2.1018.0",
    "ballvalve": "^3.1.3"
  },
  "engines": {
    "node": ">=14",
    "npm": "use pnpm",
    "pnpm": ">=6",
    "yarn": "use pnpm"
  }
}
